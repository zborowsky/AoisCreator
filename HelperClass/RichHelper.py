from rich.console import Console
from rich.style import Style

info = Style(color="green", bold=True)
info_small = Style(color="green", bold=False)
warning = Style(color="blue", bold=True)
error = Style(color="red", bold=True)
console = Console()
